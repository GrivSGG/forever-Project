# Чеклист деплоя проверки подписки

## ✅ Выполнено

- [x] Добавлен endpoint `/api/auth/verify-subscription` в API
- [x] Добавлена функция `VerifySubscription()` в loader
- [x] Добавлен поток `SubscriptionCheckThread()` в loader
- [x] Loader успешно скомпилирован (539 KB)
- [x] Проверка синтаксиса API - OK
- [x] Проверка синтаксиса Loader - OK

## 📋 Осталось сделать

### 1. Задеплоить API на Railway
```bash
cd forever-auth-api
git add server.js
git commit -m "Add real-time subscription verification endpoint"
git push
```

**Проверка:**
```bash
curl -X POST https://forever-project-production-a2fa.up.railway.app/api/auth/verify-subscription \
  -H "Content-Type: application/json" \
  -d '{"username":"admin"}'
```

### 2. Загрузить новый loader на GitHub
```bash
# В репозитории forever-Project
copy D:\cataclysm\loader\ForeverLoader.exe .
git add ForeverLoader.exe
git commit -m "Add real-time subscription verification to loader"
git push
```

### 3. Протестировать систему

#### Тест 1: Отзыв подписки
1. Создайте тестового пользователя с подпиской
2. Запустите игру через новый loader
3. Откройте helper панель
4. Отзовите подписку
5. **Ожидается:** Игра завершится в течение 1 секунды

#### Тест 2: Истечение подписки
1. Создайте подписку "1day"
2. В Firebase измените `expiresAt` на прошлую дату
3. **Ожидается:** При следующей проверке игра завершится

#### Тест 3: Бан пользователя
1. Запустите игру
2. Забаньте пользователя
3. **Ожидается:** Игра завершится в течение 1 секунды

### 4. Обновить пользователей
- Разместите новый loader на сайте
- Уведомите пользователей об обновлении
- Опционально: создайте релиз на GitHub

## 🎯 Критерии успеха

- [ ] API задеплоен и отвечает на запросы
- [ ] Loader загружен на GitHub
- [ ] Тест 1 пройден (отзыв подписки)
- [ ] Тест 2 пройден (истечение подписки)
- [ ] Тест 3 пройден (бан пользователя)

## 📊 Метрики

После деплоя проверьте:
- Время отклика API: должно быть < 500ms
- Задержка реакции: максимум 1 секунда
- Нагрузка на сервер: ~200 байт/сек на пользователя

## 🔧 Откат (если что-то пошло не так)

### Откат API
```bash
cd forever-auth-api
git revert HEAD
git push
```

### Откат Loader
Загрузите предыдущую версию loader на GitHub.

## 📝 Документация

- `ПРОВЕРКА_ПОДПИСКИ_РЕАЛЬНОГО_ВРЕМЕНИ.md` - полная техническая документация
- `ДЕПЛОЙ_ПРОВЕРКИ_ПОДПИСКИ.md` - подробная инструкция по деплою
- `ГОТОВО_ПРОВЕРКА_ПОДПИСКИ.md` - краткое резюме

## ⚠️ Важно

1. **Обязательно задеплойте API** - без этого loader не сможет проверять подписку
2. **Пользователи должны обновить loader** - старая версия не будет проверять подписку
3. **Протестируйте перед публикацией** - убедитесь, что все работает

---

**Следующий шаг:** Задеплоить API на Railway
